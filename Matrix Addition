arr=eval(input("Enter the first matrix"))
ar=eval(input("Enter the second matrix"))
print(arr)
print(ar)
res=[]
for i in range(len(arr)):
    row = []
    for j in range(len(arr[0])):
        row.append(arr[i][j] + ar[i][j])
    res.append(row)
print(res)
